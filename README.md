# Restored portfolio

Run in PowerShell:

```powershell
npm install
npm start
```

The package is self-contained. The source archive included one of the 341 requested hero frames; the homepage now uses that supplied frame safely without failed requests. All supplied page components and interactions are retained.


## V2 About / Contact
Redesigned to directly reuse the homepage's composition language: shader background, Breton/Machine type, blue accent, frame corners, eyebrow/dash labels, pill chips, fluid line, image cards, oversized editorial typography, magnetic links and pinned horizontal motion.
